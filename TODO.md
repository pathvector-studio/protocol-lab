# Protocol Lab 開発 TODO

## 未実装ラボの実装
- [x] Lab 05: DNS: Recursive Resolution with `dig` (digを使ったDNS再帰問い合わせ検証ラボの実装)
- [x] Lab 06: DNS: Caching, TTL, and Negative Answers (DNSキャッシュ、TTL、ネガティブキャッシュ検証ラボの実装)
- [x] Lab 07: TCP: Handshake, Sequence Numbers, and Teardown (TCPハンドシェイク、シーケンス番号、切断シーケンス検証ラボの実装)
- [x] Lab 08: TCP: Retransmission, Windowing, and Loss (TCP再送、ウィンドウ制御、パケットロス検証ラボの実装)
- [x] Lab 09: TLS: Handshake, Certificates, and Keys (TLSハンドシェイク、証明書、SNI/ALPN検証ラボの実装)
- [x] Lab 10: HTTP: Requests, Responses, and Caching (HTTPメソッド、ヘッダー、ステータスコード、キャッシュ検証ラボの実装)
- [x] Lab 11: HTTP/2 and QUIC: Streams and Multiplexing (HTTP/2とQUICのストリーム多重化およびトランスポート層の違い検証ラボの実装)
- [x] Lab 12: End-to-End: From Domain Name to Encrypted Web Request (DNSからTCP/QUIC、TLS、HTTPまでを繋ぐエンドツーエンド検証ラボの実装)
- [x] Lab 13: DNSSEC: Signatures, Trust Anchors, and the AD Flag (署名済みゾーンの検証、RRSIG/DNSKEY/AD フラグ、改ざん検出=SERVFAIL 検証ラボの実装)
- [x] Lab 14: Encrypted DNS: DoT and DoH (平文DNS vs DNS over TLS/HTTPSでquery名の漏洩を比較する検証ラボの実装)
- [x] Lab 15: Mutual TLS (mTLS): client 証明書認証、CertificateRequest、certless の拒否を観察する検証ラボの実装)
- [x] Lab 16: WireGuard tunnel: トンネル越しの ping が underlay では暗号化UDP、wg0 内では平文ICMP になることを観察する検証ラボの実装)
- [x] Lab 17: DANE/TLSA: DNSSEC 署名された TLSA で 証明書を pin し、CA 無しで DANE 検証と impostor 拒否を観察する検証ラボの実装)
- [x] Lab 18: VXLAN L2 overlay: VNI/UDP 4789 の encapsulation を観察し、暗号化されない内側フレームが underlay で読めることを WireGuard と対比する検証ラボの実装)
- [x] Lab 19: traceroute と TTL: 多段ルータで TTL 減算と ICMP time-exceeded により経路を hop ごとに映し出す検証ラボの実装)
- [x] Lab 20: NAT (source translation): private client が MASQUERADE 経由で public server に到達し、server が NAT の public アドレスを見ることと conntrack の変換を観察する検証ラボの実装)
- [x] Lab 21: GRE L3 tunnel: IP proto 47 の GRE encapsulation を観察し、暗号化されない内側 IP が underlay で見えることを WireGuard/VXLAN と対比する検証ラボの実装)
- [x] Lab 22: DHCP (DORA): アドレスなしの client が Discover/Offer/Request/Ack の4メッセージで lease を得る様子を capture する検証ラボの実装)
- [x] Lab 23: IPv6 Neighbor Discovery (NDP): NS を solicited-node multicast へ送り、NA で MAC を得る ARP の IPv6 版を capture する検証ラボの実装)
- [x] Lab 24: ARP (IPv4 address resolution): broadcast の ARP request と unicast reply で MAC を得る NDP の IPv4 版を capture し対比する検証ラボの実装)
- [x] Lab 25: MTU と Path MTU Discovery: DF 付きの大きすぎるパケットが ICMP frag-needed で拒否され、next-hop MTU を学んで cache する様子を capture する検証ラボの実装)
- [x] Lab 26: VLAN (802.1Q): 1本の物理リンク上の 2つの VLAN が tag(vlan 100/200)で分離される様子を capture する検証ラボの実装)
- [x] Lab 27: HTTP redirects と cookies: 302 Location の追従と Set-Cookie/Cookie の往復を curl で観察する検証ラボの実装)
- [x] Lab 28: QoS / traffic shaping: tc tbf(token bucket)でリンクを rate 制限し、iperf3 の throughput が設定 rate に落ちる様子を測る検証ラボの実装)
- [x] Lab 29: Multicast / IGMP: 1つの sender が UDP を multicast group に送り、IGMP で join した2つの receiver が同じ1本の stream を受け取る様子(IGMP report と multicast MAC を capture)を観察する検証ラボの実装)
- [x] Lab 30: TCP 輻輳制御: 同じ lossy path(netem で RTT 100ms・loss 2%・100mbit 上限)上で CUBIC と BBR を iperf3 で比較し、loss-based の CUBIC が崩れ model-based の BBR が上限付近を保つ様子(ss -ti の cwnd/pacing_rate)を観察する検証ラボの実装)
- [x] Lab 31: Anycast: 2台の FRR サーバが同じ VIP(10.0.0.100/32)を BGP で announce し、r1 が AS_PATH の短い server-a を選ぶ様子と、server-a のリンク断で server-b へ自動フェイルオーバーする様子(同一 VIP のまま応答が server-a→server-b に変わる)を観察する検証ラボの実装)
- [x] Lab 32: ECMP: r1↔r2 を2本の並行リンクで eBGP 接続(maximum-paths 2)し、r1 が2 next-hop の等コスト経路を持つ様子と、多数の並行 TCP flow が L4 hashing(fib_multipath_hash_policy=1)で両リンクにほぼ均等に分散する様子(L3 hashing では片リンクに集中)を観察する検証ラボの実装)
- [x] Lab 33: L4 ロードバランシング: IPVS の director(VIP 10.0.9.100、round-robin、NAT モード)が、client からの30接続を3つの backend に 10/10/10 で分配する様子(client は VIP しか見えない)を観察する検証ラボの実装)
- [x] Lab 34: OSPF: r1/r2/r3 が area 0 の三角形で Full 隣接を作り、SPF が cost 最短(直リンク r1-r3, cost 20)を選ぶ様子と、直リンク障害で r2 経由(cost 30)へ再収束し target 到達性が保たれる様子を観察する検証ラボの実装(link-state IGP、BGP path-vector との対比))
- [x] Lab 35: BFD: OSPF 三角形に BFD(300ms×3≈900ms)を足し、silent failure(eth2 を iptables DROP、リンクは UP のまま)を約900msで検出して r2 へ再収束する様子(OSPF 単独の 40 秒 dead timer との対比)を観察する検証ラボの実装)
- [x] Lab 36: ステートフルファイアウォール: fw の FORWARD を DROP 既定にし、conntrack で ESTABLISHED,RELATED と内側(eth1)発 NEW のみ許可。client→server は通り(内側発+応答は ESTABLISHED)、server→client の勝手な新規は落ちる様子(向きでなく conntrack の状態が決める)を観察する検証ラボの実装)
- [x] Lab 37: TCP MSS clamping: r–server リンクを MTU 1400 にし、r の mangle FORWARD で TCPMSS --clamp-mss-to-pmtu を適用。client の SYN の MSS が clamping 無しで 1460、有りで 1360(1400−40)に書き換わる様子を server 側 capture で観察する検証ラボの実装(PMTUD blackhole 対策))
- [x] Lab 38: ポリシールーティング: srcA/srcB がルータ r の後ろ、up1/up2 が同じ 10.0.100.1 を持つ。宛先ベースでは両者 up1、`ip rule from srcB lookup 200`(table 200 default via up2)を足すと srcB だけ up2 に——同じ宛先が送信元で別経路になる様子を観察する検証ラボの実装)
- [x] Lab 39: Reverse path filtering: attacker(net B)が net A の送信元(10.0.1.10)を nping --source-ip で偽装。strict rp_filter(=1)で入口 drop(target 到達 0)、本物の 10.0.2.10 は通る(到達 3)、rp_filter=0 で偽装が転送される(到達 3)様子を観察する検証ラボの実装(BCP 38 ingress filtering))
- [x] Lab 40: DNAT / ポートフォワード: gw の PREROUTING で公開 203.0.113.1:8080 を内部 private サーバ 10.0.0.2:80 に DNAT。規則前は無応答、規則後は client が公開アドレス経由で内部サーバに到達(conntrack で宛先変換を確認)する様子を観察する検証ラボの実装(SNAT lab 20 の入り方向の対))
- [x] Lab 41: DNS ラウンドロビン: BIND authoritative が web.lab. に3つの A レコード(203.0.113.11/.12/.13)+ rrset-order cyclic を持ち、連続 dig で先頭アドレスが .11→.12→.13 と巡回してクライアントを3 backend に散らす様子を観察する検証ラボの実装(anycast/ECMP/IPVS とは層が違う名前解決層の分散))
- [x] Lab 42: Split-horizon DNS: BIND の2つの view(internal=match-clients 10.0.1.0/24 → app.lab A 10.0.0.5、external=any → app.lab A 203.0.113.5)で、内部/外部クライアントが同じ app.lab を引いて別のアドレス(private/public)を得る様子を観察する検証ラボの実装(送信元で view 選択))
- [x] Lab 43: Network Namespaces を手で組む: 2つの netns(red/blue)に veth を張り 10.10.0.2/.3 を設定しても host 側の端がどこにも挿さっていないため不達(ARP INCOMPLETE、100% loss)、bridge br0 に両端を enslave した瞬間に同じ ping が通り(REACHABLE、0% loss)、bridge が2つの MAC を FDB に学習する様子を観察する検証ラボの実装(コンテナのネットワーク = netns + veth + bridge、containerlab 不要・特権コンテナ1つで完結))
- [x] Lab 44: Linux Bridge の中身: a/b/c の3 namespace を1つの br0 に載せ、a→b の unicast を傍観者 c が promiscuous で捕捉。b が未学習なら flood されて c に届き(1発)、往復で学習後は届かず(0発)、bridge fdb del で b の1行だけ消すと再び flood される(1発)。ageing_time 500(=5秒、センチ秒)で無通信12秒後にエントリが自然消滅する様子も観察する検証ラボの実装(学習は送信元・転送は宛先、IPv6無効化と静的ARPで測定を成立させる))
- [x] Lab 45: GENEVE: 1つの underlay(10.30.0.0/24)上に GENEVE(UDP 6081)と VXLAN(UDP 4789)を同じ vni 100 で並走させ、同じ ICMP がどう包まれるかを生バイトで比較。offset 0x1c から GENEVE は Ver/Opt Len + Protocol Type 0x6558 + VNI、VXLAN は同じ4バイトが予約のゼロ。内側は両方とも平文、外側 source port は内側フローのハッシュ(3フロー→3ポート)、gnv0 は MTU 1450 に対し vx0 は 1500 のまま(断片化/blackhole の罠)を観察する検証ラボの実装(RFC 8926、Lab 18 の続編))
- [x] Lab 46: XDP / eBPF: ICMP を落とす小さな XDP プログラム(検証後34命令、境界チェック必須)を bpftool でロード・アタッチし、同じ ping を XDP と iptables の2箇所で止めて比較。どちらも ping は 0/2 だが、tcpdump が見たのは XDP 時 0 発・iptables 時 2 発——XDP は sk_buff 生成前に走るためキャプチャに現れない。消えたパケットの証拠は BPF マップのカウンタ(4)で取る様子を観察する検証ラボの実装(フィルタを置く層が観測可能性を変える。要 --ulimit memlock=-1、bpffs は /sys の外に、ip netns exec が sysfs を貼り直すため))
